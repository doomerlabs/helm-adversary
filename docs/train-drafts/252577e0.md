# Train draft 252577e0

- **Package:** `helm`
- **Kind:** miss — Should have caught — human raised this; the package did not match it.
- **Title:** helm: catch defect class — The `ca.crt` path fix above is good. One related is…
- **Human gold:** The `ca.crt` path fix above is good. One related issue left in this same file.

**Problem:** `--cluster-gateway-ca-file` is set for `direct` + `secureTLS`, but the CA volume mount (`tls-cert-vol-cg`) only renders inside `admissionWebhooks.enabled`. With admission off, the controller still gets the CA arg and then crashes:

```
open /cluster-gateway-tls-cert/ca.crt: no such file or directory
```

**Where:** `charts/vela-core/templates/kubevela-controller.yaml` around the `volumeMounts` / `volumes` block (~L344-L392 on this branch). Ports and probes can stay under admission.

**Change:** gate `tls-cert-vol-cg` on multicluster + secureTLS + direct, not on admission. Something like:

```yaml
{{- $needAdmissionTLS := .Values.admissionWebhooks.enabled }}
{{- $needClusterGatewayTLS := and .Values.multicluster.enabled .Values.multicluster.clusterGateway.secureTLS.enabled .Values.multicluster.clusterGateway.direct }}

{{ if .Values.admissionWebhooks.enabled }}
ports: ...
probes: ...
{{ end }}

{{ if or $needAdmissionTLS $needClusterGatewayTLS }}
volumeMounts:
  {{ if $needAdmissionTLS }}
  - name: tls-cert-vol
    mountPath: {{ .Values.admissionWebhooks.certificate.mountPath }}
    readOnly: true
  {{ end }}
  {{ if $needClusterGatewayTLS }}
  - name: tls-cert-vol-cg
    mountPath: /cluster-gateway-tls-cert
    readOnly: true
  {{ end }}
{{ end }}

{{ if or $needAdmissionTLS $needClusterGatewayTLS }}
volumes:
  {{ if $needAdmissionTLS }}
  - name: tls-cert-vol
    secret:
      secretName: {{ template "kubevela.fullname" . }}-admission
  {{ end }}
  {{ if $needClusterGatewayTLS }}
  - name: tls-cert-vol-cg
    secret:
      secretName: {{ template "kubevela.fullname" . }}-cluster-gateway-tls-v2
  {{ end }}
{{ end }}
```

I tried this locally on k3d: `admissionWebhooks.enabled=false` + direct + secureTLS + cert-manager failed before the change and came up Ready after. Default `admissionWebhooks.enabled=true` still works.
- **PR:** https://github.com/kubevela/kubevela/pull/7193
- **Run:** `slice-1786623833511406000`

_Applied by `doomer train results apply`. Implement the generalized detection class; keep wording as evidence only._

## Miss brief — package `helm`

### Human gold (example only — do not copy verbatim)

> The `ca.crt` path fix above is good. One related issue left in this same file. **Problem:** `--cluster-gateway-ca-file` is set for `direct` + `secureTLS`, but the CA volume mount (`tls-cert-vol-cg`) only renders inside `admissionWebhooks.enabled`. With admission off, the controller still gets the CA arg and then crashes: ``` open /cluster-gateway-tls-cert/ca.crt: no such file or directory ``` **Where:** `charts/vela-core/templates/kubevela-controller.yaml` around the `volumeMounts` / `volumes` block (~L344-L392 on this branch). Ports and probes can stay under admission. **Change:** gate `tls-cert-vol-cg` on multicluster + secureTLS + direct, not on admission. Something like: ```yaml {{- $needAdmissionTLS := .Values.admissionWebhooks.enabled }} {{- $needClusterGatewayTLS := and .Values.multicluster.enabled .Values.multicluster.clusterGateway.secureTLS.enabled .Values.multicluster.clusterGateway.direct }} {{ if .Values.admissionWebhooks.enabled }} ports: ... probes: ... {{ end }} {{ if or $needAdmissionTLS $needClusterGatewayTLS }} volumeMounts: {{ if $needAdmissionTLS }} - name: tls-cert-vol mountPath: {{ .Values.admissionWebhooks.certificate.mountPath }} readOnly: true {{ end }} {{ if $needClusterGatewayTLS }} - name: tls-cert-vol-cg mountPath: /cluster-gateway-tls-cert readOnly: true {{ end }} {{ end }} {{ if or $needAdmissionTLS $needClusterGatewayTLS }} volumes: {{ if $needAdmissionTLS }} - name: tls-cert-vol secret: secretName: {{ template "kubevela.fullname" . }}-admission {{ end }} {{ if $needClusterGatewayTLS }} - name: tls-cert-vol-cg secret: secretName: {{ template "kubevela.fullname" . }}-cluster-gateway-tls-v2 {{ end }} {{ end }} ``` I tried this locally on k3d: `admissionWebhooks.enabled=false` + direct + secureTLS + cert-manager failed before the change and came up Ready after. Default `admissionWebhooks.enabled=true` still works.

- PR: https://github.com/kubevela/kubevela/pull/7193
- PR title: Fix(chart): use ca.crt CA path for cluster-gateway when cert-manager is enabled
- Case: `kubevela-pr-7193-r2`
- Anchor: `charts/vela-core/templates/kubevela-controller.yaml`

### Spirit class: `defect`

This is a **correctness / safety / real defect** concern. The package should surface an equivalent finding with evidence (file/line) on similar patterns.

### When to post this class of feedback

- There is a plausible incorrect, unsafe, or incomplete behavior in the change.
- You can point at code (path/line) or a concrete scenario that fails.
- A careful human reviewer would block or demand a fix before merge.

### When **not** to post

- The concern is speculative with no code path that can hit it.
- It is pure style dressed up as a bug.

### How to surface (implementation)

- Emit a real finding with severity appropriate to impact, evidence, and recommendation.
- Match the *class* of bug (e.g. offset validation), not the exact human wording.

### Wording: keep spirit, vary surface form

The package must **not** hard-code the human sentence above in rules. For this policy/specialist package, keep the wording as training evidence only; do **not** append each concern to `agent/voice.md`.

Possible paraphrases for the same reasoning class (evidence only, not voice-bank entries):

1. "This looks wrong — what happens on the empty/edge path?"
2. "I don't buy this invariant; prove it or fix it."
3. "That check is incomplete."
4. "(spirit of) The `ca.crt` path fix above is good. One related issue left in this same file. **Problem:*…"


### Acceptance for this train item

- [ ] On similar changes, the package emits this **class** of signal (not the exact quote)
- [ ] Posture matches **when to post** (no rubber-stamp on broken code; no silent ship on real bugs)
- [ ] Human wording remains evidence only; `agent/voice.md` is not expanded for this item
- [ ] Tests cover the **class**, not a single repository string match
